# Project Aletheia - RPi 4 Requirements
# Pinned to user-verified stable versions

# Core GUI and System
pygame==2.6.1
opencv-python==4.11.0.86

# Machine Learning & Hand Tracking
# Note: mediapipe-rpi4 is used for optimized Pi 4 bindings
mediapipe-rpi4==0.10.18
protobuf==3.20.3

# Vision and Tensors
# Pinned to match your torch 2.9.1+cpu environment
numpy==2.1.2
torch==2.9.1
torchvision

# ExecuTorch for YOLO XNNPACK
# matches your verified 1.0.1 installation
executorch==1.0.1
torchao==0.14.0